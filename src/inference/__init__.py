"""Deploy-time inference for the clot baseline (GNODE teacher + clot-phi readout).

Import explicitly, e.g. ``from src.inference.clot_baseline import ClotBaselinePredictor``.
CLI: ``python -m src.inference`` or ``python -m src.bin.main eval clot-baseline``.
"""
