# Archived scripts

Retired launchers and one-off surveys from pre-`biochem_gnn` ladders (GNODE, clot-ML rules, T0, clot-phi sweeps, graybox S0–S3, M3 summarize helpers).

**Do not treat these as the supported public surface.** Active entry points live in `scripts/README.md` and `AGENTS.md`.

Recover history via git if a script is needed; prefer re-implementing against `src/biochem_gnn/` rather than reviving deleted trainers.
